@model List<Cart>

@{
    ViewBag.Title = "Zamówienia";
    Layout = "_Layout";
}

@if (Model.Count > 0)
{
    <h2>Zamówienia użytkownika @Model[0].UserId</h2>
    <table class="table">
        @foreach (var cart in @Model)
        {
            <tr>
                <th colspan="4">Zamówienie nr. @cart.Id</th>
            </tr>
            @foreach (var product in @cart.Products)
            {
                <tr>
                    <td>@product.Id</td>
                    <td>@product.Name</td>
                    <td>@product.Price zł</td>
                    <td>@product.Count szt</td>
                </tr>
            }
            <tr class="table-info">
                <td colspan="4">Suma: @cart.Sum zł</td>
            </tr>
            <tr style="height: 20px"></tr>
        }
    </table>
}
else
{
    <h2>Brak zamówień</h2>
}
<style>
</style>
